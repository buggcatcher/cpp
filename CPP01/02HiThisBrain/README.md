### Exercise 02: HI THIS IS BRAIN

**Turn-in directory:** `ex02/`  
**Files to turn in:** `Makefile`, `main.cpp`  
**Forbidden functions:** None

#### Objective
Demystify references and pointers in C++ by printing addresses and values.

#### Requirements
- Create a string variable initialized to `"HI THIS IS BRAIN"`.
- Create `stringPTR`: a pointer to the string.
- Create `stringREF`: a reference to the string.
- Print:
  - The memory address of the string variable.
  - The memory address held by `stringPTR`.
  - The memory address held by `stringREF`.
- Then print:
  - The value of the string variable.
  - The value pointed to by `stringPTR`.
  - The value pointed to by `stringREF`.

---